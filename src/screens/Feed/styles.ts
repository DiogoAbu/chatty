import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    fontSize: 24,
  },
  fabUpper: {
    position: 'absolute',
    right: 30,
    bottom: 90,
  },
});

export default styles;
